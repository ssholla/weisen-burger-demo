import { LightningElement, api } from 'lwc';

export default class ConstructionProjectMap extends LightningElement {
    // @api projects = []; // optional array of project objects with coordinates

    // Placeholder for future map integration (Leaflet/Mapbox/Google Maps)
}